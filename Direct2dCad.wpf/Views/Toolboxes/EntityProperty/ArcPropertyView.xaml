<UserControl x:Class="Direct2dCad.wpf.Views.Toolboxes.EntityProperty.ArcPropertyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:r="clr-namespace:Direct2dCad.Lang;assembly=Direct2dCad.Lang"
             xmlns:vm_ep="clr-namespace:Direct2dCad.ViewModels.Toolboxes.EntityProperty;assembly=Direct2dCad.ViewModels"
             xmlns:ep="clr-namespace:Direct2dCad.wpf.Views.Toolboxes.EntityProperty"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm_ep:ArcPropertyViewModel}"
             d:DesignHeight="620"
             d:DesignWidth="320">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="12" IsEnabled="{Binding IsEditable, Mode=OneWay}">
            <TextBlock Text="{I18N {x:Static r:LangKeys.Arc}}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
            <ep:EntityHeaderPropertySection ViewModel="{Binding}" />
            <ep:EntitySettingsPropertySection ViewModel="{Binding}" />
            <ep:StrokeAppearancePropertySection ViewModel="{Binding}" />
            <Expander Header="{I18N {x:Static r:LangKeys.Coordinates}}" IsExpanded="True" Margin="0,0,0,8">
                <Grid Margin="8,6,8,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="96" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.CenterX}}" VerticalAlignment="Center" />
                    <mah:NumericUpDown Grid.Row="0" Grid.Column="1" Value="{Binding CenterX, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" StringFormat="F3" Interval="1" Margin="0,0,0,8" />
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.CenterY}}" VerticalAlignment="Center" />
                    <mah:NumericUpDown Grid.Row="1" Grid.Column="1" Value="{Binding CenterY, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" StringFormat="F3" Interval="1" Margin="0,0,0,8" />
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.Radius}}" VerticalAlignment="Center" />
                    <mah:NumericUpDown Grid.Row="2" Grid.Column="1" Value="{Binding Radius, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Minimum="0.001" StringFormat="F3" Interval="1" Margin="0,0,0,8" />
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.Start}}" VerticalAlignment="Center" />
                    <mah:NumericUpDown Grid.Row="3" Grid.Column="1" Value="{Binding StartAngleDegrees, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" StringFormat="F2" Interval="5" Margin="0,0,0,8" />
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.Sweep}}" VerticalAlignment="Center" />
                    <mah:NumericUpDown Grid.Row="4" Grid.Column="1" Value="{Binding SweepAngleDegrees, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Minimum="-360" Maximum="360" StringFormat="F2" Interval="5" Margin="0,0,0,8" />
                    <TextBlock Grid.Row="5" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.End}}" VerticalAlignment="Center" />
                    <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding EndAngleDegrees, Mode=OneWay, StringFormat=F2}" IsReadOnly="True" Margin="0,0,0,8" />
                    <CheckBox Grid.Row="6" Grid.ColumnSpan="2" Content="{I18N {x:Static r:LangKeys.CounterClockwise}}" IsChecked="{Binding IsCounterClockwise}" />
                </Grid>
            </Expander>
            <ep:StrokeStylePropertySection ViewModel="{Binding}" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
