<UserControl x:Class="Direct2dCad.wpf.Views.Toolboxes.EntityProperty.SplinePropertyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:r="clr-namespace:Direct2dCad.Lang;assembly=Direct2dCad.Lang"
             xmlns:vm_ep="clr-namespace:Direct2dCad.ViewModels.Toolboxes.EntityProperty;assembly=Direct2dCad.ViewModels"
             xmlns:ep="clr-namespace:Direct2dCad.wpf.Views.Toolboxes.EntityProperty"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm_ep:SplinePropertyViewModel}"
             d:DesignHeight="760" d:DesignWidth="360">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="12">
            <TextBlock Text="{I18N {x:Static r:LangKeys.Spline}}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
            <ep:EntityHeaderPropertySection ViewModel="{Binding}" />
            <CheckBox Content="{I18N {x:Static r:LangKeys.Closed}}"
                      IsChecked="{Binding IsClosed}"
                      Margin="0,2,0,8" />

            <ep:EntitySettingsPropertySection ViewModel="{Binding}" />
            <ep:FillPropertySection ViewModel="{Binding}" />
            <ep:StrokeAppearancePropertySection ViewModel="{Binding}" />
            <Expander Header="{I18N {x:Static r:LangKeys.Coordinates}}" IsExpanded="True">
                <Grid Margin="8,6,8,8">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="96" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.FitPoints}}" VerticalAlignment="Top" Margin="0,4,0,0" />
                    <DataGrid Grid.Row="0" Grid.Column="1" ItemsSource="{Binding FitPoints, Mode=OneWay}" SelectedItem="{Binding SelectedFitPoint, Mode=TwoWay}"
                              AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column" MaxHeight="180" Margin="0,0,0,8">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" Binding="{Binding Index, Mode=OneWay}" IsReadOnly="True" Width="36" />
                            <DataGridTemplateColumn Header="{I18N {x:Static r:LangKeys.X}}" Width="*">
                                <DataGridTemplateColumn.CellTemplate><DataTemplate><mah:NumericUpDown Value="{Binding X, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" StringFormat="F3" Interval="1" Margin="0" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="{I18N {x:Static r:LangKeys.Y}}" Width="*">
                                <DataGridTemplateColumn.CellTemplate><DataTemplate><mah:NumericUpDown Value="{Binding Y, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" StringFormat="F3" Interval="1" Margin="0" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="{I18N {x:Static r:LangKeys.AddAfter}}" Command="{Binding AddFitPointAfterSelectedCommand}" Margin="0,0,8,0" />
                        <Button Content="{I18N {x:Static r:LangKeys.Remove}}" Command="{Binding RemoveSelectedFitPointCommand}" />
                    </StackPanel>
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.PointCount}}" VerticalAlignment="Center" />
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding FitPointCount, Mode=OneWay}" IsReadOnly="True" Margin="0,0,0,8" />
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.Length}}" VerticalAlignment="Center" />
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Length, Mode=OneWay, StringFormat=F3}" IsReadOnly="True" />
                </Grid>
            </Expander>
            <ep:StrokeStylePropertySection ViewModel="{Binding}" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
