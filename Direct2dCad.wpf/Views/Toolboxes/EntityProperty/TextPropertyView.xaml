<UserControl x:Class="Direct2dCad.wpf.Views.Toolboxes.EntityProperty.TextPropertyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:r="clr-namespace:Direct2dCad.Lang;assembly=Direct2dCad.Lang"
             xmlns:vm_ep="clr-namespace:Direct2dCad.ViewModels.Toolboxes.EntityProperty;assembly=Direct2dCad.ViewModels"
             xmlns:ep="clr-namespace:Direct2dCad.wpf.Views.Toolboxes.EntityProperty"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm_ep:TextPropertyViewModel}"
             d:DesignHeight="760" d:DesignWidth="320">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="12">
            <TextBlock Text="{I18N {x:Static r:LangKeys.Text}}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
            <ep:EntityHeaderPropertySection ViewModel="{Binding}" />
            <ep:EntitySettingsPropertySection ViewModel="{Binding}" />
            <Expander Header="{I18N {x:Static r:LangKeys.Text}}" IsExpanded="True" Margin="0,0,0,8">
                <Grid Margin="8,6,8,8">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="96" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.Content}}" VerticalAlignment="Top" Margin="0,4,0,0" />
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding TextContent, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="64" Margin="0,0,0,8" />
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.Font}}" VerticalAlignment="Center" />
                    <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding FontFamilyOptions, Mode=OneWay}" SelectedItem="{Binding SelectedFontFamily, Mode=TwoWay}" IsTextSearchEnabled="True" Margin="0,0,0,8" />
                    <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,2,0,8">
                        <CheckBox Content="{I18N {x:Static r:LangKeys.Inverted}}" IsChecked="{Binding IsInverted}" />
                    </StackPanel>
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.Margin}}" VerticalAlignment="Center" />
                    <mah:NumericUpDown Grid.Row="3" Grid.Column="1" Value="{Binding InvertedMarginFactor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Minimum="0" StringFormat="F3" Interval="0.01" />
                </Grid>
            </Expander>
            <ep:StrokeAppearancePropertySection ViewModel="{Binding}" />
            <Expander Header="{I18N {x:Static r:LangKeys.Coordinates}}" IsExpanded="True" Margin="0,0,0,8">
                <Grid Margin="8,6,8,8">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="96" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.PositionX}}" VerticalAlignment="Center" />
                    <mah:NumericUpDown Grid.Row="0" Grid.Column="1" Value="{Binding PositionX, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" StringFormat="F3" Interval="1" Margin="0,0,0,8" />
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.PositionY}}" VerticalAlignment="Center" />
                    <mah:NumericUpDown Grid.Row="1" Grid.Column="1" Value="{Binding PositionY, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" StringFormat="F3" Interval="1" Margin="0,0,0,8" />
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.Height}}" VerticalAlignment="Center" />
                    <mah:NumericUpDown Grid.Row="2" Grid.Column="1" Value="{Binding Height, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Minimum="0.001" StringFormat="F3" Interval="1" Margin="0,0,0,8" />
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.Rotation}}" VerticalAlignment="Center" />
                    <mah:NumericUpDown Grid.Row="3" Grid.Column="1" Value="{Binding RotationDegrees, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" StringFormat="F2" Interval="5" Margin="0,0,0,8" />
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.Bounds}}" VerticalAlignment="Center" />
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding BoundsSizeText, Mode=OneWay}" IsReadOnly="True" />
                </Grid>
            </Expander>
        </StackPanel>
    </ScrollViewer>
</UserControl>
