<UserControl x:Class="Direct2dCad.wpf.Views.Toolboxes.EntityProperty.CommonEntityPropertyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ep="clr-namespace:Direct2dCad.wpf.Views.Toolboxes.EntityProperty"
             xmlns:vm="clr-namespace:Direct2dCad.ViewModels.Toolboxes.EntityProperty;assembly=Direct2dCad.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:CommonEntityPropertyViewModel}"
             d:DesignHeight="520"
             d:DesignWidth="320"
             mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="12" IsEnabled="{Binding IsEditable, Mode=OneWay}">
            <TextBlock Margin="0,0,0,10"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Text="{Binding Title, Mode=OneWay}" />
            <ep:EntityHeaderPropertySection ViewModel="{Binding}" />
            <ep:EntitySettingsPropertySection ViewModel="{Binding}" />
            <ep:StrokeAppearancePropertySection
                ViewModel="{Binding}"
                Visibility="{Binding SupportsStrokeAppearance, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <ep:StrokeStylePropertySection
                ViewModel="{Binding}"
                Visibility="{Binding SupportsStrokeStyle, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
