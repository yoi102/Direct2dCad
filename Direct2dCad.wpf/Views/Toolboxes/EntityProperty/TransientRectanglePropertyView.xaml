<UserControl x:Class="Direct2dCad.wpf.Views.Toolboxes.EntityProperty.TransientRectanglePropertyView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls" xmlns:r="clr-namespace:Direct2dCad.Lang;assembly=Direct2dCad.Lang" xmlns:ep="clr-namespace:Direct2dCad.wpf.Views.Toolboxes.EntityProperty">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><StackPanel Margin="12">
        <TextBlock Text="{I18N {x:Static r:LangKeys.RectangleDrawing}}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
        <ep:DrawingLayerPropertySection ViewModel="{Binding}" /><ep:EntitySettingsPropertySection ViewModel="{Binding}" /><ep:FillPropertySection ViewModel="{Binding}" /><ep:StrokeAppearancePropertySection ViewModel="{Binding}" />
        <Expander Header="{I18N {x:Static r:LangKeys.Coordinates}}" IsExpanded="True" Margin="0,0,0,8">
            <Grid Margin="8,6,8,8"><Grid.ColumnDefinitions><ColumnDefinition Width="96" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.CornerX}}" VerticalAlignment="Center" /><mah:NumericUpDown Grid.Row="0" Grid.Column="1" Value="{Binding CornerRadiusX, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Minimum="0" StringFormat="F3" Interval="1" Margin="0,0,0,8" />
                <TextBlock Grid.Row="1" Grid.Column="0" Text="{I18N {x:Static r:LangKeys.CornerY}}" VerticalAlignment="Center" /><mah:NumericUpDown Grid.Row="1" Grid.Column="1" Value="{Binding CornerRadiusY, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Minimum="0" StringFormat="F3" Interval="1" />
            </Grid>
        </Expander>
        <ep:StrokeStylePropertySection ViewModel="{Binding}" />
    </StackPanel></ScrollViewer>
</UserControl>
