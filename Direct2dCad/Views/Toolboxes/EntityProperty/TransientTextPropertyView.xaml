<UserControl x:Class="Direct2dCad.wpf.Views.Toolboxes.EntityProperty.TransientTextPropertyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:r="clr-namespace:Direct2dCad.Lang;assembly=Direct2dCad.Lang"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:cv="clr-namespace:Direct2dCad.wpf.Converters"
             xmlns:vm_ep="clr-namespace:Direct2dCad.ViewModels.Toolboxes.EntityProperty;assembly=Direct2dCad.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm_ep:TransientTextPropertyViewModel}"
             d:DesignHeight="430"
             d:DesignWidth="320">
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="12">
            <TextBlock Text="Text drawing"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Margin="0,0,0,10" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="96" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Grid.Column="0"
                           Text="Content"
                           VerticalAlignment="Top"
                           Margin="0,4,0,0" />
                <TextBox Grid.Row="0"
                         Grid.Column="1"
                         Text="{Binding TextContent, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         MinHeight="64"
                         Margin="0,0,0,8" />

                <TextBlock Grid.Row="1"
                           Grid.Column="0"
                           Text="Style"
                           VerticalAlignment="Center" />
                <ComboBox Grid.Row="1"
                          Grid.Column="1"
                          ItemsSource="{Binding TextStyleOptions, Mode=OneWay}"
                          SelectedItem="{Binding SelectedTextStyleOption, Mode=TwoWay}"
                          DisplayMemberPath="Name"
                          Margin="0,0,0,8" />

                <TextBlock Grid.Row="2"
                           Grid.Column="0"
                           Text="Color"
                           VerticalAlignment="Center" />
                <mah:ColorPicker Grid.Row="2"
                                 Grid.Column="1"
                                 mah:TextBoxHelper.AutoWatermark="True"
                                 mah:TextBoxHelper.ClearTextButton="True"
                                 AdvancedTabHeader="{I18N {x:Static r:LangKeys.ColorPickerAdvanced}}"
                                 ColorPalettesTabHeader="{I18N {x:Static r:LangKeys.ColorPickerPalettes}}"
                                 AvailableColorPaletteHeader="{I18N {x:Static r:LangKeys.ColorPickerAvailableColors}}"
                                 RecentColorPaletteHeader="{I18N {x:Static r:LangKeys.ColorPickerRecentColors}}"
                                 StandardColorPaletteHeader="{I18N {x:Static r:LangKeys.ColorPickerStandardColors}}"
                                 AddToRecentColorsTrigger="SelectedColorChanged"
                                 SelectedColor="{Binding StrokeColor, Mode=TwoWay, Converter={x:Static cv:CadColorToMediaColorConverter.Instance}}"
                                 Margin="0,0,0,8" />

                <TextBlock Grid.Row="3"
                           Grid.Column="0"
                           Text="Line weight"
                           VerticalAlignment="Center" />
                <mah:NumericUpDown Grid.Row="3"
                                   Grid.Column="1"
                                   Value="{Binding LineWeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                   Minimum="0.01"
                                   StringFormat="F3"
                                   Interval="0.05"
                                   Margin="0,0,0,8" />

                <TextBlock Grid.Row="4"
                           Grid.Column="0"
                           Text="Draw order"
                           VerticalAlignment="Center" />
                <mah:NumericUpDown Grid.Row="4"
                                   Grid.Column="1"
                                   Value="{Binding ZIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                   StringFormat="F0"
                                   Interval="1"
                                   Margin="0,0,0,8" />

                <StackPanel Grid.Row="5"
                            Grid.ColumnSpan="2"
                            Orientation="Horizontal"
                            Margin="0,2,0,10">
                    <CheckBox Content="Visible"
                              IsChecked="{Binding IsVisible}"
                              Margin="0,0,16,0" />
                    <CheckBox Content="Inverted"
                              IsChecked="{Binding IsInverted}" />
                </StackPanel>

                <TextBlock Grid.Row="6"
                           Grid.Column="0"
                           Text="Margin"
                           VerticalAlignment="Center" />
                <mah:NumericUpDown Grid.Row="6"
                                   Grid.Column="1"
                                   Value="{Binding InvertedMarginFactor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                   Minimum="0"
                                   StringFormat="F3"
                                   Interval="0.01"
                                   Margin="0,0,0,8" />

            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
